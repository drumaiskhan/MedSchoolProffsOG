[build]
  base = "/"
  command = "pnpm --filter @workspace/frontend-admin run build"
  publish = "frontend-admin/dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_FLAGS = "--no-frozen-lockfile"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
