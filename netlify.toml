[build]
  base = "/"
  command = "pnpm --filter @workspace/medschoolproffs build"
  publish = "artifacts/medschoolproffs/dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_FLAGS = "--no-frozen-lockfile"
