[build]
  base = "/"
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/medschoolproffs build"
  publish = "artifacts/medschoolproffs/dist"

[build.environment]
  NODE_VERSION = "22"
