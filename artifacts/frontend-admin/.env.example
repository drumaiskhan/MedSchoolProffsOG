# Base URL of the backend API (the api-server package), e.g. https://api.medschoolproffs.com
VITE_API_BASE_URL=

# URL of the student frontend, used to redirect a student account away from
# this (admin-only) app if they sign in here by mistake.
VITE_STUDENT_URL=
