# Base URL of the backend API (the api-server package), e.g. https://api.medschoolproffs.com
VITE_API_BASE_URL=

# URL of the admin frontend, used to redirect an admin account away from this
# (student-only) app if they sign in here by mistake.
VITE_ADMIN_URL=
